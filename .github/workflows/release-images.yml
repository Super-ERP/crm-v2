name: release-images

on:
  workflow_dispatch:
    inputs:
      ref:
        description: Release tag to build (must be annotated semantic version, e.g. v1.2.3)
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: release-images-${{ github.event.inputs.ref }}
  cancel-in-progress: false

env:
  IMAGE_NAMESPACE: ghcr.io/super-erp
  TARGET_REF: ${{ github.event.inputs.ref }}

jobs:
  quality:
    uses: ./.github/workflows/quality.yml
    with:
      ref: ${{ github.event.inputs.ref }}
      run_build: false

  # Tag validation is independent of the test suite. Run it in parallel so
  # release builds can start while quality checks finish.
  validate-tag:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    outputs:
      source_commit: ${{ steps.tag.outputs.source_commit }}
    steps:
      - name: Check out annotated release tag
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ github.event.inputs.ref }}
          fetch-depth: 0
          fetch-tags: true
          persist-credentials: false
      - name: Require strict SemVer annotated tag
        id: tag
        shell: bash
        run: |
          set -euo pipefail
          if [[ ! "$TARGET_REF" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "release tag must be strict SemVer: vMAJOR.MINOR.PATCH" >&2
            exit 1
          fi
          if [ "$(git cat-file -t "$TARGET_REF")" != "tag" ]; then
            echo "release tag must be annotated: $TARGET_REF" >&2
            exit 1
          fi
          echo "source_commit=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

  build:
    needs: validate-tag
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    permissions:
      contents: read
      packages: write
      id-token: write
    env:
      SOURCE_COMMIT: ${{ needs.validate-tag.outputs.source_commit }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: web
            repository: crm-web
            context: .
            file: Dockerfile
            target: runner
          - name: migrator
            repository: crm-migrator
            context: .
            file: Dockerfile
            target: migrator
          - name: backup
            repository: crm-backup
            context: docker/backup
            file: docker/backup/Dockerfile
            target: ""
            trivyIgnore: docker/backup/.trivyignore
          - name: agent
            repository: crm-deployment-agent
            context: .
            file: apps/deployment-agent/Dockerfile
            target: runtime
    steps:
      - name: Check out release source
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ env.SOURCE_COMMIT }}
          persist-credentials: false
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0
        with:
          version: v0.34.1
      - name: Log in to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Install Cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
        with:
          cosign-release: v3.0.6
      - name: Build and push untagged immutable digest
        id: build-push
        uses: docker/build-push-action@f9f3042f7e2789586610d6e8b85c8f03e5195baf # v7.2.0
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.file }}
          target: ${{ matrix.target }}
          platforms: linux/amd64
          tags: ${{ env.IMAGE_NAMESPACE }}/${{ matrix.repository }}
          outputs: type=image,push-by-digest=true,name-canonical=true,push=true
          provenance: mode=max
          sbom: true
          # Keep independent scopes so concurrent matrix jobs do not overwrite
          # one another. The GitHub token makes cache lookup use the API path
          # instead of the cache service's slower lookup path.
          cache-from: type=gha,scope=${{ matrix.name }},ghtoken=${{ secrets.GITHUB_TOKEN }},repository=${{ github.repository }}
          # A cache outage must not fail an otherwise valid signed release.
          cache-to: type=gha,mode=max,scope=${{ matrix.name }},timeout=20m,ignore-error=true
      - name: Validate immutable image reference
        id: image
        shell: bash
        env:
          IMAGE_NAME: ${{ env.IMAGE_NAMESPACE }}/${{ matrix.repository }}
          DIGEST: ${{ steps.build-push.outputs.digest }}
        run: |
          set -euo pipefail
          if [[ ! "$DIGEST" =~ ^sha256:[0-9a-f]{64}$ ]]; then
            echo "build did not return an immutable digest: $DIGEST" >&2
            exit 1
          fi
          echo "ref=${IMAGE_NAME}@${DIGEST}" >> "$GITHUB_OUTPUT"
      - name: Scan digest with Trivy
        uses: aquasecurity/trivy-action@a9c7b0f06e461e9d4b4d1711f154ee024b8d7ab8 # v0.36.0
        with:
          image-ref: ${{ steps.image.outputs.ref }}
          version: v0.70.0
          scan-type: image
          scanners: vuln,secret
          format: table
          exit-code: "1"
          trivyignores: ${{ matrix.trivyIgnore || '' }}
          ignore-unfixed: true
          severity: HIGH,CRITICAL
      - name: Prepare evidence directory
        shell: bash
        run: mkdir -p release
      - name: Generate SPDX JSON SBOM from digest
        id: sbom
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0
        with:
          image: ${{ steps.image.outputs.ref }}
          format: spdx-json
          output-file: release/${{ matrix.name }}.spdx.json
          syft-version: v1.42.3
          upload-artifact: false
          upload-release-assets: false
      - name: Sign and verify immutable digest
        shell: bash
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          WORKFLOW_IDENTITY: https://github.com/${{ github.repository }}/.github/workflows/release-images.yml@refs/heads/main
        run: |
          set -euo pipefail
          cosign sign --yes "$IMAGE_REF"
          cosign verify \
            --certificate-identity "$WORKFLOW_IDENTITY" \
            --certificate-oidc-issuer "https://token.actions.githubusercontent.com" \
            --output=json \
            "$IMAGE_REF" > "release/${{ matrix.name }}.cosign-verification.json"
      - name: Publish version and commit tags after verification
        shell: bash
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_NAME: ${{ env.IMAGE_NAMESPACE }}/${{ matrix.repository }}
        run: |
          set -euo pipefail
          docker buildx imagetools create \
            --tag "${IMAGE_NAME}:${{ env.TARGET_REF }}" \
            --tag "${IMAGE_NAME}:sha-${SOURCE_COMMIT}" \
            "$IMAGE_REF"
      - name: Record immutable image metadata
        shell: bash
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_NAME: ${{ env.IMAGE_NAMESPACE }}/${{ matrix.repository }}
          DIGEST: ${{ steps.build-push.outputs.digest }}
          WORKFLOW_IDENTITY: https://github.com/${{ github.repository }}/.github/workflows/release-images.yml@refs/heads/main
        run: |
          set -euo pipefail
          jq -n \
            --arg name "${{ matrix.name }}" \
            --arg image "$IMAGE_NAME" \
            --arg image_ref "$IMAGE_REF" \
            --arg digest "$DIGEST" \
            --arg source_commit "$SOURCE_COMMIT" \
            --arg workflow_identity "$WORKFLOW_IDENTITY" \
            --arg build_time "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
            '{name: $name, image: $image, image_ref: $image_ref, digest: $digest, source_commit: $source_commit, workflow_identity: $workflow_identity, build_time: $build_time}' \
            > "release/${{ matrix.name }}-image.json"
      - name: Upload image evidence
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-${{ matrix.name }}
          path: |
            release/${{ matrix.name }}.spdx.json
            release/${{ matrix.name }}.cosign-verification.json
            release/${{ matrix.name }}-image.json
          if-no-files-found: error
          retention-days: 90

  # Keep quality as a release gate, but do not serialize it before image builds.
  manifest:
    needs: [build, validate-tag, quality]
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      actions: write
      contents: write
      id-token: write
    env:
      SOURCE_COMMIT: ${{ needs.validate-tag.outputs.source_commit }}
    steps:
      - name: Check out release bundle
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ env.SOURCE_COMMIT }}
          persist-credentials: false
      - name: Download image evidence
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: release-*
          path: release-inputs
      - name: Compose release manifest
        shell: bash
        env:
          WORKFLOW_IDENTITY: https://github.com/${{ github.repository }}/.github/workflows/release-images.yml@refs/heads/main
        run: |
          set -euo pipefail
          RELEASE_VERSION="${TARGET_REF#v}"
          MIGRATION_VERSION="$(jq -r '.entries[-1].tag' apps/web/db/migrations/meta/_journal.json | cut -d_ -f1)"
          jq -s \
            --arg release_tag "$TARGET_REF" \
            --arg release_version "$RELEASE_VERSION" \
            --arg migration_version "$MIGRATION_VERSION" \
            --arg source_commit "$SOURCE_COMMIT" \
            --arg workflow_identity "$WORKFLOW_IDENTITY" \
            --arg build_time "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
            '{release_tag: $release_tag, release_version: $release_version, migration_version: $migration_version, source_commit: $source_commit, workflow_identity: $workflow_identity, build_time: $build_time, images: sort_by(.name)}' \
            release-inputs/release-*/*-image.json \
            > release-manifest.json
          jq -e '
            .source_commit == env.SOURCE_COMMIT and
            .workflow_identity == env.WORKFLOW_IDENTITY and
            (.release_version | test("^[0-9]+\\.[0-9]+\\.[0-9]+$")) and
            (.migration_version | test("^[0-9]{4}$")) and
            (.images | length == 4) and
            ([.images[].name] | sort == ["agent", "backup", "migrator", "web"]) and
            all(.images[]; (.digest | test("^sha256:[0-9a-f]{64}$")))
          ' release-manifest.json >/dev/null
      - name: Upload release manifest
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-manifest-${{ env.TARGET_REF }}
          path: release-manifest.json
          if-no-files-found: error
          retention-days: 90
      - name: Package source-free client deployment bundle
        shell: bash
        run: |
          set -euo pipefail
          cp release-manifest.json deploy/client/release-manifest.json
          chmod 0700 deploy/client/*.sh deploy/client/ops/*.sh
          tar --sort=name --owner=0 --group=0 --numeric-owner --mtime='UTC 1970-01-01' \
            -czf client-deployment-bundle.tar.gz -C deploy client
      - name: Install Cosign for bundle signing
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
        with:
          cosign-release: v3.0.6
      - name: Sign source-free deployment bundle
        shell: bash
        run: |
          set -euo pipefail
          cosign sign-blob --yes \
            --bundle client-deployment-bundle.sigstore.json \
            client-deployment-bundle.tar.gz
      - name: Upload client deployment bundle
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: client-deployment-bundle-${{ env.TARGET_REF }}
          path: |
            client-deployment-bundle.tar.gz
            client-deployment-bundle.sigstore.json
          if-no-files-found: error
          retention-days: 90
      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release view "$TARGET_REF" >/dev/null 2>&1 || gh release create "$TARGET_REF" --verify-tag --generate-notes --title "$TARGET_REF"
      - name: Deploy signed release to staging
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run deploy-staging.yml --ref main -f release_run_id="$GITHUB_RUN_ID"
